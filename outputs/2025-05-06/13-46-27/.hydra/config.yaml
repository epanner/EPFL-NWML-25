preprocessing:
  steps:
  - _target_: fft_filter
    filter_order: 4
    low_freq: 0.5
    high_freq: 30
    fs: 250
  - _target_: identity
model:
  hidden_channels: 64
  num_layers: 3
  dropout: 0.2
batch_size: 128
num_epochs: 4
seed: 42
learning_rate: 0.001
prefetch_dataset: false
